<!-- SPDX-FileCopyrightText: 2026 Alethia Labs <legal@alethialabs.io> -->
<!-- SPDX-License-Identifier: AGPL-3.0-only -->

# Provisioning e2e — run history (append-only)

Every `scripts/e2e/provisioning-e2e.sh` run — and the nightly `rollup` job — appends one row here (newest
at the bottom) and writes a scrubbed proof bundle under `demos/proofs/<cloud>/<stamp>/`. This is the durable
audit trail; git history is the timeline. Parity board:
[`docs/testing/provisioning-e2e-parity.md`](../../docs/testing/provisioning-e2e-parity.md).

- **dimension**: `floor` (provision + cluster_ready + ArgoCD converge — and **nothing else**: no
  day-2 soak, no drift check) · `maxconfig` (all 11 kinds) · `addons` (all 18 add-ons Healthy+Synced) ·
  `gitops` (A0.6: an apps-destination repo + a bring-your-own **Helm chart**, each converging as an
  ArgoCD Application managing at least one real resource) · `byo-iac` (the seven-job custody chain:
  a customer **OpenTofu** root module refused when unsafe, applied through the state proxy, drifted,
  healed, destroyed, state cleared) · `day2` (the A0.3 soak: liveness, drift posture, PVC) ·
  `full` (every dimension in one apply **except** those declared in `FULL_EXCLUDES`).

  **`gitops` was called `byo`, and the rename is a correction.** The old name, plus a programme
  column labelled "BYO-IaC", made three cells read as proven customer-OpenTofu while proving A0.6 —
  the discrepancy this legend and `provisioning-e2e.sh` had both already recorded in prose. `byo`
  remains an accepted alias and every row filed under it keys onto the `gitops` column unchanged:
  the rows were true, the label was not, and retracting correct evidence is the worse error.
  `byo-iac` is the proof the old column claimed; it had never executed in CI.

  The list above is not prose: it is `DIMENSIONS` in `scripts/e2e/resolve-dimension.sh`, which also
  owns the env each one switches on (`fidelity_env`) and self-tests the pair. Two things this legend
  used to get wrong, both fixed in #2356 — it claimed a **`teardown`** dimension that
  `provisioning-e2e.sh` has always rejected (teardown is asserted on *every* run, so it is a property,
  not a choice), and it said 19 add-ons where the catalogue SSOT says **18** (cert-manager moved to
  the platform rail in #1722). A dimension nobody can pass and a count nobody can hit are how a
  legend stops being read.
- **verdict**: `PASS` · `FAIL` · `BLOCKED` (couldn't run — record why) · `RETRACTED` (an
  append-only correction of a prior record). A `RETRACTED` row must name the superseded row and
  explain the evidence that invalidates it; it never erases the original record.
- **bundle**: proof path (or `nightly-<run_id>` for a scheduled run, or `—` for a manual pre-helper note).

| UTC date | git sha | cloud | dimension | verdict | detail | bundle | issue |
|----------|---------|-------|-----------|---------|--------|--------|-------|
| 2026-07-22 | 8c53441 | aws | floor | **FAIL** | apply OK (124 res) but runner 401s on EKS API — access-entry ↔ IAM-path mismatch; teardown destroyed=false | `nightly-29895597616` | #1040 |
| 2026-07-22 | 8c53441 | gcp | floor | **PASS** | nightly green — provision + cluster_ready + ArgoCD converge (cheapest shape) | `nightly-29895597616` | — |
| 2026-07-22 | 8c53441 | azure | floor | **PASS** | nightly green — provision + cluster_ready + ArgoCD converge (cheapest shape) | `nightly-29895597616` | — |
| 2026-07-22 | 8c53441 | alibaba | floor | **PASS** | nightly green (out of the 3-cloud FULLY-TESTED program; tracked for parity) | `nightly-29895597616` | — |
| 2026-07-22 | 8c53441 | hetzner | floor | **PASS** | nightly green (out of the 3-cloud FULLY-TESTED program; tracked for parity) | `nightly-29895597616` | — |
| 2026-07-31 | 8c53441 | gcp | floor | **RETRACTED** | Supersedes the 2026-07-22 GCP/floor PASS: `nightly-29895597616` recorded an explicit gate-off SKIP (`E2E_GCP_WIF_PROVIDER` unset); no provision or proof bundle ran. | `nightly-29895597616` | #1723 |
| 2026-07-31 | 8c53441 | azure | floor | **RETRACTED** | Supersedes the 2026-07-22 Azure/floor PASS: `nightly-29895597616` recorded an explicit gate-off SKIP; no provision or proof bundle ran. | `nightly-29895597616` | #1723 |
| 2026-07-31 | 8c53441 | alibaba | floor | **RETRACTED** | Supersedes the 2026-07-22 Alibaba/floor PASS: `nightly-29895597616` recorded an explicit gate-off SKIP; no provision or proof bundle ran. | `nightly-29895597616` | #1723 |
| 2026-07-31 | 8c53441 | hetzner | floor | **RETRACTED** | Supersedes the 2026-07-22 Hetzner/floor PASS: `nightly-29895597616` recorded an explicit gate-off SKIP; no provision or proof bundle ran. | `nightly-29895597616` | #1723 |

<!-- provisioning-e2e.sh appends new rows below this line -->
| 2026-08-05 | fa093c24 | hetzner | addons | **FAIL** | ArgoCD gate: 12/20 apps not Healthy+Synced within 8m. Cluster provisioned and reachable (7 nodes Ready, k8s 1.35.6, signed receipt verified); addon-reloader and metrics-server BOTH converged, so #1714 and #1722 are verified. addon-loki cannot render at all (#2058). Teardown swept to zero. | `demos/proofs/hetzner/20260805T064043Z` | #2058 |
| 2026-08-24 | 2fb9607d | hetzner | floor | **FAIL** | --- FAIL: TestT2RealCloudProvisioning (633.26s) | `demos/proofs/hetzner/20260824T191327Z` | — |
| 2026-08-24 | 2fb9607d | aws | floor | **FAIL** | --- FAIL: TestT2RealCloudProvisioning (0.00s) | `demos/proofs/aws/20260824T193149Z` | — |
| 2026-08-24 | 2fb9607d | aws | floor | **RETRACTED** | Supersedes the 2026-08-24 aws/floor FAIL at `demos/proofs/aws/20260824T193149Z`. That run never reached a cloud: the harness stopped at its own prerequisite gate — "AWS credentials are not configured — set ALETHIA_E2E_AWS_READY=1 ... and ensure AWS_ACCESS_KEY_ID or AWS_ROLE_ARN is present" — because the driving shell held credentials in a profile rather than in the environment. 0.00s, no apply, no resources. The row describes the operator, not aws. The real run is the 2418.10s row below, from the same evening with the same code. | `demos/proofs/aws/20260824T193149Z` | #2459 |
| 2026-08-24 | 2fb9607d | aws | floor | **FAIL** | --- FAIL: TestT2RealCloudProvisioning (2418.10s) | `demos/proofs/aws/20260824T193315Z` | — |
| 2026-08-24 | 49325749 | hetzner | floor | **PASS** | Retry of the 20260824T191327Z FAIL, same evening, same code. Talos image built; 2 nodes Ready (k8s v1.35.6); signed ed25519 receipt sealed to plan sha256 acc72b6a…; 209 log lines shipped to job_logs; all 3 derived ArgoCD Applications Healthy+Synced (addon-reloader, external-secrets-operator, metrics-server); env reached ACTIVE via the real finalizeDeployment. Teardown: 18 destroyed, account verified empty across every resource type. | `demos/proofs/hetzner/20260824T201636Z` | — |
| 2026-08-24 | 49325749 | hetzner | addons | **FAIL** | ArgoCD gate: 12/20 Applications not Healthy+Synced within 19m30s on the heavy profile (6x cx33). PROGRESS vs 2026-08-05: addon-loki now RENDERS and installs, so #2058 is confirmed fixed. One clear ordering bug — addon-harbor SyncError calling the ingress-nginx validating admission webhook (x509: certificate signed by unknown authority) while ingress-nginx is still Progressing: any add-on creating an Ingress must not sync before that webhook serves. Six others were merely Progressing, so the 19m30s budget may be short for this dimension. Teardown: 33 destroyed, but NINE CSI volumes (140 GB) survived and were swept by hand — the local engine did not run the sweeper (fixed in #2491). | `demos/proofs/hetzner/20260824T203543Z` | #2490 |
| 2026-08-24 | 9283c11e | aws | floor | **PASS** | Re-run after #2469. The CNI now attaches its ENIs: node scaled 1->2 at seven pods (the warm spare) and on to three, where the previous run was pinned at one and the ArgoCD application-controller never got a pod sandbox. Signed ed25519 receipt sealed to plan sha256 17006c0d...; 624 log lines shipped to job_logs; node Ready on k8s v1.35.5-eks; all 4 derived ArgoCD Applications Healthy+Synced (addon-reloader, aws-load-balancer-controller, external-secrets-operator, metrics-server); env ACTIVE via the real finalizeDeployment. Teardown: 122 destroyed, account verified empty (no clusters, instances, NAT, ELB, non-default VPC or volumes). | `demos/proofs/aws/20260824T211529Z` | — |
| 2026-08-24 | 2c96dc00 | hetzner | day2 | **FAIL** | The soak's own words: "posture is not in-sync right after a clean apply: in_sync=false drifted=5". The five are provider HYDRATION, not drift — hcloud_firewall, two hcloud_primary_ip, talos_cluster_kubeconfig, talos_machine_secrets, on a cluster six minutes old that nobody touched. The DETECT_DRIFT job itself SUCCEEDED and reported honestly; t2_soak.go gates on DriftInSync being true, which a clean apply cannot deliver. The drift check is a t.Fatalf at phase 2 of 4, so the PVC and add-on phases NEVER RAN — the summary's pvc "n/a" and addons re-read=false are UNREACHED, not failed. One assertion that a clean apply cannot satisfy prevents two thirds of the day-2 bar from ever executing. Liveness passed 1/1. Teardown clean and the scope-locked sweep ran automatically (#2491's first real exercise). | `demos/proofs/hetzner/20260824T215146Z` | — |
| 2026-08-25 | b315da0a | azure | floor | **PASS** | Azure's first proven proof cell, and the first dispatched from `dev` through the `e2e-dev` environment rather than a promotion to `main`. 29 resources added in germanywestcentral (cluster alethia-nl-32816061207-1); node Ready and all derived ArgoCD Applications Healthy+Synced, both T2-asserted; signed ed25519 receipt sealed to plan sha256 f2e0f35d…; add-on wave 1 (addon-reloader) converged Healthy+Synced; deploy_stage argocd-ready in 1227s. One real observation worth keeping: the external-secrets webhook was not servable for ~45s after ArgoCD installed it, so the ClusterSecretStore apply retried three times before succeeding — the runner's own retry absorbed it, but it is the same "a wave gated on CRDs cannot see a webhook" shape as the Harbor ordering bug in #2490. NOTE ON THE RED RUN CONCLUSION: run 32816061207 finished `failure`, and that is NOT a floor failure — the only failing step is the CLI-only demo bar, which fails on the `dns-delegation` cloud ceiling (#1773) on every cloud by design. This is exactly the #2356 trap of reading a red run as a red floor. Teardown succeeded and the account was verified empty afterwards: no AKS clusters, no ephemeral resource group, 0 of 10 regional vCPU in use. | `demos/proofs/azure/20260825T063447Z` | — |
| 2026-08-25 | 8bbfe9fc | gcp | floor | **PASS** | GCP's first proven proof cell, and the first ever obtained on this cloud — every prior attempt died before reaching a cluster, because the project was linked to a CLOSED billing account (`open: false`) whose 403s the metadata reads never saw. Billing was reopened this morning and `roles/cloudkms.admin` + `alethiaE2eProjectReader` applied from #2295, clearing #2258's `secrets-encryption.tf` cause. 24 resources in europe-west3 (cluster alethia-nl-32836114196-1); node Ready and all derived ArgoCD Applications Healthy+Synced, both T2-asserted; signed ed25519 receipt sealed to plan sha256 c4b75d72…; argocd-ready in 2611s. **The A0.6 ArgoCD-with-repos assertion passed here for the first time**, over a real credentialed apps repo AND a private BYO chart repo — the run 90 minutes earlier failed it with `addon-byo-e2e: health=Missing … namespaces "byo-e2e" not found`, which #2535 fixed by having the runner create the namespace the hardened AppProject forbids the chart from creating. **day-2 access also passed** (`endpoint surfaced=true · kube reachable=true · authorized (can-i * / *) · nodes ready=4`), its first execution on any cloud. One observation kept: the external-secrets webhook was unservable for ~5 minutes (20 retries of 15s) before the ClusterSecretStore applied — far worse than azure's 3 retries, and the same "a wave gated on CRDs cannot see a webhook" class as #2490. Absorbed by the runner's own retry, so not a failure, but it is the widest margin observed yet. Teardown succeeded and the account was verified empty afterwards: no GKE clusters, no compute instances, and only the `default` network remaining. | `demos/proofs/gcp/20260825T105829Z` | — |
| 2026-08-25 | 7050809b | hetzner | full | **BLOCKED** | Two independent causes, neither a product defect, and BLOCKED rather than FAIL because neither is something a fix in this repo can clear. **(1) The DNS zone's TLD.** `hcloud_zone` was refused with `unsupported tld (invalid_input)`, Field: name, 422. Probed directly against the live API afterwards: `probe-hcloud.alethialabs.io` and `probe1.e2e.alethialabs.io` are both refused while `alethia-probe-zz19.de` and `alethia-probe-zz19.com` are created — so it is the TLD, not subdomain depth, and Hetzner simply does not host `.io`. The max-config fixture provisions `dns` at `<run>.e2e.alethialabs.io` (`maxConfigDomainSuffix`), which is the only domain we own. This is NOT recorded as a cloud ceiling and the max-config table is deliberately unchanged: hetzner genuinely carries the `dns` kind via `hcloud_zone` (maxconfig.go:793), so `CloudCeiling` would be false and `DeferredInProduct` requires a chart — neither fits. What we have is a PROOF limitation, blocked on acquiring a domain on a TLD Hetzner hosts (maintainer, 2026-08-25: "won't be this week"). Filed as #2568, which also ships the user-facing warning so a customer with a `.io` domain is told before deploying rather than after (#2570). **(2) nbg1 capacity.** Seven servers failed `error during placement (resource_unavailable)` — one control plane and six workers on the heavy profile. Environmental and retryable in `fsn1`/`hel1`, which the workflow's `region` input already accepts; not retried here because cause (1) blocks the bar regardless. Teardown succeeded and the account was verified empty independently of the sweeper: no servers, no zones, no volumes. Note the run got this far only because HETZNER_S3_ACCESS_KEY/SECRET_KEY were wired earlier the same day — before that the bar refused at the prerequisite gate, before any spend. | `demos/proofs/hetzner/20260825T130348Z` | #2568 |
| 2026-08-25 | 29d4074e | hetzner | full | **RETRACTED** | Supersedes the 2026-08-25 `7050809b` hetzner/full **BLOCKED** row (bundle `demos/proofs/hetzner/20260825T130348Z`, #2568). The two causes it records are real and its refusal to widen the max-config table is right; the VERDICT is not. `BLOCKED` is defined in this file's own legend as "couldn't run" and in `programme-rollup.mjs:35` as "the harness refused before spending" — kept distinct from `FAIL` because, in that file's words, "one spent money and broke, the other refused before spending". This run spent. Its own committed `provision-summary.json` says `"outcome": "failure"`, `"deploy_stage": "applying"`, `"duration_seconds": 237` and `"verdict": "hetzner: ❌ FAILED at stage 'applying'"`; `run.log` records `Plan: 36 to add` followed by **19 `Creation complete` lines** — `hcloud_network` id 12579024, six `hcloud_primary_ip`, a Talos image-factory schematic and a 50s `imager_image` build — and ends `Error: tofu apply failed: exit status 1`. A cell that reached `applying` and broke is `FAIL`; recording it `BLOCKED` also ranks it BELOW never-run cells in the actionable list, so a run that cost money reads as one that never started. Re-recorded on the next row. | `demos/proofs/hetzner/20260825T130348Z` | #2568 |
| 2026-08-25 | 29d4074e | hetzner | full | **FAIL** | Re-record of the run superseded above, same bundle and same two causes, with the verdict its own summary reports. Died at `applying` after 237s with 19 of 36 resources created. **(1) The DNS zone's TLD.** `hcloud_zone` refused with `unsupported tld (invalid_input)`, Field: name, 422. Probed against the live API afterwards (probe zones deleted): `probe-hcloud.alethialabs.io` and `probe1.e2e.alethialabs.io` both refused while `alethia-probe-zz19.de` and `alethia-probe-zz19.com` were created — the TLD, not subdomain depth. The max-config fixture provisions `dns` at `<run>.e2e.alethialabs.io` (`maxConfigDomainSuffix`), the only domain we own. Deliberately NOT a cloud ceiling and the max-config table stays unchanged: hetzner genuinely carries the `dns` kind via `hcloud_zone` (`maxconfig.go:793`), so `CloudCeiling` would be false and `DeferredInProduct` requires a chart. It is a PROOF limitation, blocked on a domain whose TLD Hetzner hosts (maintainer, 2026-08-25: "won't be this week"). Filed as #2568, which also ships the user-facing warning (#2570, gate corrected in #2573). Note the console gate does NOT protect this path: T2 seeds `config_snapshot` straight into `jobs` (`t2_provision_test.go:841`) and never calls `buildConfigSnapshot`, so the nightly will spend to the same 422 again until the harness carries the check or the fixture changes domain. **(2) nbg1 capacity.** Seven servers failed `error during placement (resource_unavailable)` — one control plane, six workers on the heavy profile. Environmental and retryable in `fsn1`/`hel1`; not retried because cause (1) blocks the bar regardless. **Teardown, unresolved:** the prose of the superseded row reports teardown succeeded and the account verified empty of servers, zones and volumes — but that enumeration does not cover the two kinds this run actually created and left mid-apply (`hcloud_network`, `hcloud_primary_ip`), and the summary records `"destroyed": false`. That flag may simply predate the teardown, since the summary is captured at failure. Not asserted either way here: it needs one look with the e2e token at `alethia-nl-32850686520-1-*`. | `demos/proofs/hetzner/20260825T130348Z` | #2568 |
| 2026-08-25 | 0087536b | azure | full | **FAIL** | Azure Managed Redis cannot be created on this subscription at all, so the `cache` kind cannot provision and the bar cannot pass here regardless of anything in this repo. FAIL rather than BLOCKED, though the cause is an account limitation rather than a defect: BLOCKED is defined as "the harness refused before spending" (provisioning-e2e-log.md:24, programme-rollup.mjs:35) and this run spent — it provisioned a real AKS cluster and reached `deploy_stage: applying` over 1724s before the cache failed. The distinction is load-bearing rather than pedantic: deriveCell hardcodes "refused before spending" into every BLOCKED rationale, so the wrong verdict prints a false sentence into the generated half, and ⛔ ranks BELOW never-run cells — a run that cost money would rank under cells nobody has attempted. Run 32853960703 (westeurope, after #2552) got all the way to `applying` with only ONE error left — `InsufficientCapacity` on `azurerm_managed_redis` — where the previous attempt (32836351919, germanywestcentral) had failed on three: #2552 fixed the Cosmos partition-key rooting (`pk` is a DynamoDB attribute name, Cosmos wants a `/path`) and moved the region off germanywestcentral, which reports `supportedServerVersions: 0` for Postgres Flexible Server where westeurope reports 8. Both of those are proven fixed by this run reaching Redis at all. **Three probes, each ruling out one explanation** (all cleaned up; `az group list` verified back to the four permanent groups): `Balanced_B1`, what max-config resolves, fails in BOTH germanywestcentral and westeurope, so it is not the region; `Balanced_B0`, the smallest SKU Azure offers, is accepted and then goes `provisioningState: Failed / resourceState: CreateFailed`, so it is not our SKU choice; `Balanced_B0` with `highAvailability: Disabled` fails identically, so it is not zone availability either — which was a real suspect, since the default `redundancyMode: ZR` needs three zones a student subscription may not have. What remains is the subscription: "Azure for Students" cannot allocate Managed Redis. Note the create is ACCEPTED and fails ASYNCHRONOUSLY, which is why it costs a 26-minute apply to discover and why no plan-time check can catch it. **The max-config table is deliberately unchanged** — Azure genuinely offers Managed Redis and `azurerm_managed_redis` is the correct resource (the module comment records why it is chosen over the provider-deprecated `azurerm_redis_enterprise_cluster`), so dropping the kind would be widening a ceiling to clear a red. This is an account limitation: a non-student subscription is a maintainer spend decision. Teardown succeeded and the account was verified empty independently of the sweeper, enumerating what THIS run created rather than a fixed list: no AKS clusters, no Cosmos accounts, and the ephemeral resource group gone — in Azure the resource group is the containment boundary, so nothing inside it survived and the flagged orphan risk did not materialise. The two probe resource groups were deleted and `az group list` confirmed back to the four permanent ones. | `demos/proofs/azure/20260825T140000Z` | — |
| 2026-08-25 | bbcfcc3 | hetzner | byo | **PASS** | hetzner's second proven cell, and the first proof bundle ever captured by CI rather than by a laptop. The A0.6 ArgoCD-with-repos assertion passed over a real credentialed apps repo AND a private BYO chart repo: `repo-apps` + `repo-byo` (addon-byo-e2e) derived, credentialed, Healthy+Synced and managing real resources on real infra. 18 resources applied in nbg1 (cluster alethia-nl-32878485034-1); 2 nodes Ready and all derived ArgoCD Applications Healthy+Synced, both T2-asserted; signed ed25519 receipt sha256 40a09cc1d232; deploy_stage argocd-ready in 1155s. day-2 ACCESS also passed (endpoint surfaced, kube reachable, authorized `can-i * / *`, nodes ready=2). **This run only reached a cluster because of #2625.** The run 48 minutes earlier (32873754809) died at CNI bootstrap on every attempt: `env.HCLOUD_LOAD_BALANCERS_USE_PRIVATE_IP.value` was rendered as a YAML boolean, server-side apply refused the CCM Deployment, and because `bootstrap_manifests` is ONE apply that took Cilium down with it — so no Hetzner cluster of any dimension had come up since #2536 landed that morning. **What this row does NOT claim.** `byo-iac: skipped` and `byo-iac-summary.json` records `enabled: false`: the BYO-IaC scenario (ALETHIA_E2E_BYO_IAC, t2_byo_iac.go) did not run. The `byo` DIMENSION's fidelity is A0.6 and only A0.6 — `resolve-dimension.sh` emits `ALETHIA_E2E_ARGO_REPOS_REQUIRE=1` and nothing else, and its own comment calls A0.6 "the only assertion it has". But PROGRAMME.md's grid names this column "BYO-IaC" and describes it as "customer IaC/charts applied, and Alethia services bound to their outputs", which is the OTHER scenario. Those two definitions are not the same thing and one of them should move; recorded here rather than resolved, because deciding it silently in a ledger row is how a cell gets promoted by assertion. **Teardown.** `provision-summary.json` says `destroyed: false`, and that flag is captured AT failure/summary time, before the sweep finishes — the same ambiguity left unresolved on the 2026-08-25 hetzner/full row. Resolved here by independent enumeration with the e2e token AFTER the run: servers, volumes, load balancers, networks, primary IPs, SSH keys and zones are ALL zero. Nothing standing. **The run is marked `failure` and the cell is not.** Its only failing step is the CLI-only demo bar, which fails on the `dns-delegation` cloud ceiling (#1773) on every cloud by design — the #2356 trap of reading a red run as a red cell. `TestT2RealCloudProvisioning` reports `--- PASS (1020.64s)`. | `demos/proofs/hetzner/2026-08-25T175213Z` | — |
| 2026-08-25 | 70b0c863 | aws | full | **FAIL** | aws's first full bar ever to reach a cloud, and it died on an assertion that had already been fixed while it ran. Run 32883119943, 52 minutes, FAILED at stage `argocd-ready` after 2637s. **What it proved before failing, which is most of the bar.** 258 managed resource instances in state. `FT-5 max-config on aws: 11 kind(s) proven in tofu state [network cluster database cache queue topic nosql secrets bucket registry dns]` — the entire provisionable surface, including Aurora, ElastiCache and the ACM/Route53 path that #2630 had to unblock to let this run start at all. A0.6 ArgoCD-with-repos proven; every expected ArgoCD Application Healthy+Synced; `Deployment completed successfully`. **Why it failed:** `A0.3 drift: posture is not in-sync right after a clean apply: in_sync=false drifted=37`. That is the #2503 gate, and #2636 replaced it with a baseline-delta the same evening — this run was dispatched at 18:19Z from a dev that did not yet carry it. The 37 are the same provider-hydration class hetzner showed at a fifth of the scale: `attachment_count` and `tags` on IAM policies, `managed_policy_arns` on IAM roles, `egress`/`ingress` on security groups, `cluster_members` on the Aurora cluster. 13 more were dismissed as `empty_collection` by the normalizer, which is working exactly as designed. **This row is therefore NOT evidence that aws cannot pass the full bar** — it is evidence that it very nearly did. **Two defects it exposed that are NOT the cause and remain open.** (1) The expected ArgoCD set was SIX Applications `[addon-byo-e2e apps aws-load-balancer-controller external-dns external-secrets-operator metrics-server]`, not the 18 marketplace charts, with the same `recordAddonHealth did not persist real ArgoCD health` warning hetzner showed — so the add-ons half of this run asserted the floor. #2642 makes that a hard failure instead of a silent one; until it lands, no `full` or `addons` verdict on any cloud should be read as covering the 18 charts. (2) `ClusterSecretStore` was refused twice with `no matches for kind ... in external-secrets.io/v1beta1`, absorbed by the runner's retry. Whether that is a CRD race or an API-version drift in the ESO chart is unresolved and needs its own look. **Teardown verified independently of the sweeper**, enumerating what this run created rather than a fixed list: 0 EKS clusters, 0 running instances, 0 NAT gateways, 0 RDS clusters, 0 ElastiCache replication groups, 0 load balancers, and 0 ACM certificates — the last confirming #2614's new ACM sweep on its first real exercise. | `demos/proofs/aws/2026-08-25T190929Z` | #2642 |
| 2026-08-25 | f3cb966 | hetzner | day2 | **PASS** | hetzner's third proven cell, and the first time the day-2 bar has ever run to completion on any cloud. `--- PASS: TestT2RealCloudProvisioning (1657.88s)`. Soak summary: `liveness 20/20 ok · drift SUCCESS baseline=5 final=5 NEW=0 (state=27 res, non-empty reads=3) · pvc bound=true sweep-tag=true (vol 106704371) · addons re-read=true`. **The drift gate now measures the right thing (#2636).** Baseline and final are the SAME five entries — `hcloud_firewall.this` (apply_to), two `hcloud_primary_ip` (assignee_id), and the two wholly-sensitive talos resources whose leaves are not computable. Nothing new appeared during the window, which is what day-2 actually asks. The old gate demanded `in_sync=true`, which this template cannot deliver because those attributes are declared from the other side of their relationship; `normalize.go` was right to refuse to dismiss them and is untouched. **Two phases executed for the first time ever.** The old gate was a `t.Fatalf` at phase 2 of 4, so the PVC and add-on phases had never once run — their `n/a` in every prior summary was UNREACHED, not passed, exactly as the 2026-08-24 row complained. Both ran here: a 1Gi PVC Bound to `pvc-1f994966…`, and its backing hcloud volume 106704371 verified to carry `cluster=alethia-nl-32886071207-1` — the CSI-PVC-leak guard (BYOC G2) finally exercised against a real volume, and the add-on health re-read returned true. **Teardown verified independently of the sweeper, and the CSI volume is the point:** servers, volumes, load balancers, networks, primary IPs, SSH keys and zones are all zero. A dynamically-provisioned `pvc-*` volume is precisely the resource that survives a cluster delete, so `volumes=0` is the evidence that the sweep tag the run just asserted actually did its job. **The run is marked `failure` and the cell is not** — its only failing step is the CLI-only demo bar, which fails on the `dns-delegation` ceiling (#1773) on every cloud by design (#2356). | `demos/proofs/hetzner/20260825T192100Z` | — |
| 2026-08-25 | 09911316 | gcp | byo | **PASS** | gcp's second proven cell, and the A0.6 bring-your-own proof's second cloud. `--- PASS: TestT2RealCloudProvisioning (2725.79s)`. `A0.6: ArgoCD-with-repos proven — repo-apps + repo-byo Applications Healthy+Synced and managing real resources on real infra`, over a real credentialed apps repo AND a private BYO chart repo, on GKE. Expected ArgoCD set `[addon-byo-e2e apps external-secrets-operator]`, all Healthy+Synced. **Same caveat as hetzner/byo, restated rather than assumed away:** this proves A0.6, NOT the BYO-IaC scenario. The `byo` dimension's fidelity is `ALETHIA_E2E_ARGO_REPOS_REQUIRE=1` and nothing else; PROGRAMME.md's grid calls this column "BYO-IaC" and describes a different scenario (`t2_byo_iac.go`). Two definitions, one name — recorded, not resolved. **A free finding from the pre-spend preflight, which reported UNKNOWN rather than passing quietly:** `"europe-west3" is a region, not a zone — machine-type availability is zonal, so nothing was checked`. The workflow exports a REGION-shaped `ALETHIA_E2E_REGION` for gcp while the provider row's default is the ZONE `europe-west3-a`, so the gcp capacity check has never actually run. It refused to answer rather than answering wrongly, which is the intended behaviour — but the gate is inert on this cloud until the shapes agree. Filed separately; not fixed here, because changing the region a run provisions into is not a thing to slip into a proof row. **Teardown verified independently of the sweeper:** 0 GKE clusters, 0 compute instances, 0 disks, and only the `default` network remaining. The disk count is the one that matters on gcp — a GKE node pool's PDs are what survive a cluster delete. **The run is marked `failure` and the cell is not** — only the CLI-only demo bar failed, on the `dns-delegation` ceiling (#1773), by design on every cloud (#2356). | `demos/proofs/gcp/20260825T200519Z` | — |
| 2026-08-26 | 0ae54a34 | azure | byo | **PASS** | azure's second proven cell, and A0.6's THIRD cloud — the bring-your-own apps-repo/chart proof now holds on hetzner, gcp and azure. `--- PASS: TestT2RealCloudProvisioning (1862.45s)`. `A0.6: ArgoCD-with-repos proven — repo-apps + repo-byo Applications Healthy+Synced and managing real resources on real infra`, over a real credentialed apps repo AND a private BYO chart repo, on AKS. Expected ArgoCD set `[addon-byo-e2e apps external-secrets-operator]`, all Healthy+Synced. **Ran BEFORE #2672 landed**, so the A0.6 injection still replaced the seeded add-on list rather than appending to it — the `reloader` seed was dropped. That does not touch this claim (A0.6 is what the `byo` dimension asserts, and it is what passed) but it is why the expected set is three Applications and not four. The same is true of the hetzner and gcp byo rows above; recorded here once for all three. **Same definitional caveat as those rows:** this proves A0.6, NOT the BYO-IaC scenario (`t2_byo_iac.go`), which PROGRAMME.md's grid names this column after. **The pre-spend preflight is INERT on azure, and said so:** `[UNKNOWN]: could not ask the cloud whether "Standard_D2s_v3" is available in westeurope (az: signal: killed)`. `az vm list-skus` enumerates every SKU in the region and exceeded the probe's 30s bound, so the capacity gate has never actually run on azure either — the second cloud where it is inert, after gcp's region/zone mismatch (#2686). It reported UNKNOWN and proceeded rather than refusing a good run or claiming a check it did not make, which is the intended behaviour of the third verdict. Filed separately. **Teardown verified independently of the sweeper:** 4 resource groups (the permanent ones only), 0 AKS clusters, 0 regional vCPU in use. In Azure the resource group is the containment boundary, so a count of exactly four is the strong form of the check. **The run is marked `failure` and the cell is not** — only the CLI-only demo bar failed, on the `dns-delegation` ceiling (#1773), by design on every cloud (#2356). | `demos/proofs/azure/20260825T210320Z` | — |
| 2026-08-26 | 09911316 | gcp | day2 | **PASS** | gcp's third proven cell, and the day-2 bar's second cloud. `--- PASS: TestT2RealCloudProvisioning (3386.44s)`. Soak summary: `liveness 19/19 ok · drift SUCCESS baseline=1 final=1 NEW=0 (state=27 res, non-empty reads=3) · pvc: n/a (provider cloud-side check not wired) · addons re-read=true`. **The delta model holds across three very different hydration profiles without tuning.** gcp's baseline is ONE entry; hetzner's was five; aws's was thirty-seven. An absolute `in_sync` gate would have needed a different answer on each cloud and got none of them right; `NEW=0` is the same statement everywhere. That is the argument for #2636 made by measurement rather than by reasoning. **The PVC phase is a documented DEFERRAL here, not a pass:** the soak logged `cloud-side sweep-tag check is not wired for provider gcp yet (deferred to BYOC A1.2/A2.x)`. `PVCChecked=false`, so it does not gate the verdict — the same posture `soakVerdictPass` already applies to a check that did not run. Recorded so this row is not read as covering the CSI-leak guard, which on this cloud it does not. The teardown sweep separately reports `CSI pvc-* disks: none`. **Teardown verified independently of the sweeper:** 0 GKE clusters, 0 compute instances, 0 disks, only the `default` network remaining. **The run is marked `failure` and the cell is not** — only the CLI-only demo bar failed, on the `dns-delegation` ceiling (#1773), by design on every cloud (#2356). | `demos/proofs/gcp/20260825T210602Z` | — |
| 2026-08-26 | 66b98126 | azure | day2 | **PASS** | azure's third proven cell, and the day-2 bar's third cloud. `--- PASS: TestT2RealCloudProvisioning (2596.66s)`. Soak summary: `liveness 19/19 ok · drift SUCCESS baseline=1 final=1 NEW=0 (state=32 res, non-empty reads=3) · pvc: n/a (provider cloud-side check not wired) · addons re-read=true`. **A FOURTH hydration profile, and the delta still needs no tuning.** Baselines now measured: azure 1, gcp 1, hetzner 5, aws 37. The absolute `in_sync` gate #2636 replaced would have required a different answer on each of the four. **The azure capacity preflight ran for the first time, and it ran on the case that motivated fixing it:** `[PROCEED]: "Standard_D2s_v3" is available in westeurope (the cloud lists 1114 available type(s) there) — NOTE: restricted in zone(s) 1, 3, so a zone-pinned deployment there would still fail`. All three halves of #2716 are visible in that one line — the 90s bound enumerated all 1114 SKUs where the previous 30s bound was killed every time; the type-aware predicate returned PROCEED where `length(restrictions)==0` would have REFUSED a run that then passed; and the zone caveat is carried rather than flattened into a bare "available". **PVC is a documented deferral here, as on gcp** — the cloud-side sweep-tag check is not wired for azure, so `PVCChecked=false` and it does not gate. This row does not cover the CSI-leak guard. **Teardown verified independently of the sweeper:** 4 resource groups (the permanent ones only), 0 AKS clusters, 0 managed disks, 0 regional vCPU in use. **The run is marked `failure` and the cell is not** — only the CLI-only demo bar failed, on the `dns-delegation` ceiling (#1773), by design on every cloud (#2356). | `demos/proofs/azure/20260825T235236Z` | — |
| 2026-08-26 | d75143fc | hetzner | addons | **FAIL** | The first HONEST measurement of the 18-add-on surface. Every `addons` and `full` verdict earlier on 2026-08-25 was vacuous — #2672 fixed an A0.6 injection that replaced the seeded add-on list instead of appending to it, so those runs installed ONE chart while reporting the sweep. This run asserted **22 Applications** and applied all four waves (4 + 10 + 4 + 1), which is the surface finally being real. `11/22 expected ArgoCD Applications are not Healthy+Synced` within the derived 19m30s budget (base 6m + 18 x 45s). **The 11 are FOUR different problems, not one, and they should not be given one cause:** (a) **Healthy but OutOfSync** — `addon-argo-rollouts`, `addon-kyverno`, `addon-tempo`. The workload is up and ArgoCD still sees a diff, which is the spurious-diff class the Application template's `ignoreDifferences` already covers for `.status/terminatingReplicas` and the resourceFieldRef divisor. Three charts sitting Healthy/OutOfSync says there is at least one MORE field of that kind, and it costs the run its verdict without anything being wrong in the cluster. (b) **Progressing** — `addon-falco`, `addon-kube-prometheus-stack`, `addon-loki`, `addon-vault`. These are the heavy charts, and Progressing at the deadline is consistent with the budget simply being short. NOT asserted as such: Progressing is also what a chart that will never converge looks like at any finite deadline. (c) **Degraded** — `addon-external-dns`, `addon-harbor`, `addon-minio`. Real failures, and harbor is notable: #2536 gave the CCM a Load Balancer location, which the tree records as the true root cause of #2490, and harbor is STILL failing. Whatever is wrong with it now is not the missing LB location. (d) **Missing** — `addon-velero`, `addon-byo-e2e`. Never synced at all, which is a different failure from converging badly. **Comparable to the last honest run.** 2026-08-24 recorded `12/20 not Healthy+Synced within 19m30s` on the same dimension. 11/22 now, with two more Applications in the set. So the intervening day's green-looking runs bought no progress on this cell — they were measuring one chart. **Teardown verified independently of the sweeper:** servers, volumes, load balancers, networks, primary IPs, SSH keys and zones all zero, after a run that provisioned 7 heavy nodes and 19 add-on Applications. | `demos/proofs/hetzner/20260825T213816Z` | #2717 |
| 2026-08-26 | d75143fc | aws | full | **FAIL** | aws's first full bar with a real add-on surface, and the run that proves the add-on failures are NOT a cloud problem. 84 minutes. `11/24 expected ArgoCD Applications are not Healthy+Synced` — an **identical set** to hetzner/addons 32896755214 an hour earlier: argo-rollouts, external-dns, falco, harbor, kube-prometheus-stack, kyverno, loki, minio, tempo, vault, velero. **Eleven charts, two clouds with nothing in common** — different CNI, storage class, load-balancer implementation and node OS (Talos vs AL2023). The three cells that differ (kube-prometheus-stack, tempo, minio) all move WITHIN a class rather than across it, which is consistent with sampling an 84-minute run and a 59-minute one at different moments; nothing crosses Degraded to Healthy or back. So each chart's cause looks chart-intrinsic. Recorded against #2717, whose framing this changes: none of the four failure classes should be investigated per-cloud, and harbor's Degraded is now doubly not #2490's chain — that was a hetzner CCM Load Balancer story and harbor fails identically on EKS. **One difference that matters:** `addon-byo-e2e` is Healthy/Synced HERE and Missing on hetzner, so the `Missing` class splits — velero is cloud-independent, the BYO chart is hetzner-specific. **This run is not comparable to the 2026-08-25 aws/full row above.** That one died at the soak on the pre-#2636 gate having asserted a four-Application add-on set; this one carries #2636, #2642 and #2672 and asserts twenty-four. The earlier row's max-config result (11 kinds in tofu state) stands and is not re-litigated here. **Teardown verified independently of the sweeper after an 84-minute full bar:** 0 EKS clusters, 0 running instances, 0 NAT gateways, 0 RDS clusters, 0 ElastiCache replication groups, 0 load balancers, 0 ACM certificates. | `demos/proofs/aws/20260825T220300Z` | #2717 |
| 2026-08-26 | 66b98126 | aws | byo | **FAIL** | Every assertion the dimension makes PASSED; the run failed on a teardown timeout. Recorded FAIL because the harness says so — `--- FAIL: TestT2RealCloudProvisioning (3248.80s)` — and calling it PASS over that would be promoting a cell by assertion, which this file forbids. The cell therefore reads `failing` and ranks high for a re-drive, which is the right ranking: a re-run is cheap and likely green. **What passed:** `A0.6: ArgoCD-with-repos proven — repo-apps + repo-byo Applications Healthy+Synced and managing real resources on real infra`, over the credentialed apps repo and the private BYO chart repo. Expected set `[addon-byo-e2e addon-reloader apps aws-load-balancer-controller cert-manager external-dns external-secrets-operator metrics-server]` — note `addon-reloader` is PRESENT, which is #2672's fix visible on a real run: before it, the A0.6 injection replaced the seeded list and the reloader seed was silently dropped from every byo run (see the hetzner, gcp and azure byo rows above, all of which asserted a set without it). **What failed:** `tofu destroy failed: signal: interrupt` after `aws_internet_gateway.this[0]: Still destroying... 13m30s elapsed`. The in-test destroy ran out of context while AWS was detaching the gateway; nothing about the product. **Teardown resolved, and by evidence rather than by trust.** The workflow's `always()` sweeper is the documented guarantee and it ran. Enumerated afterwards: 0 EKS clusters, 0 running instances, 0 NAT gateways, 0 non-default VPCs, 0 Elastic IPs, 0 load balancers. ONE internet gateway survives — `igw-0ab1e5b71d1708251`, attached to a VPC whose `IsDefault` is true and carrying no tags. That is the account's permanent default-VPC gateway, and it is a DIFFERENT id from the one this run was destroying (`igw-0f72ac7592e2d55d4`), so the surviving resource is identified rather than assumed harmless. **#2716's third fix is visible here too:** the failure line reads `workflow aws-cleanup.sh is the guarantee`, where it previously hardcoded hetzner's sweeper on every cloud — misdirecting a reader at the exact moment they are diagnosing a possible leak. | `demos/proofs/aws/20260826T001739Z` | — |
| 2026-08-26 | 66b98126 | aws | day2 | **FAIL** | The run that ANSWERED #2591, and it answered against the obvious hypothesis. day-2 access failed with `argocd-url reachable=false ... dns-not-resolving: the hostname does not resolve — external-dns has not written the record (waiting longer will NOT fix this) (after 19 attempt(s) over 3m0s)`. #2591 asked whether the 3m probe budget was short or whether the record was never written, said the two were indistinguishable from `reachable=false` alone, and proposed a run at a raised timeout as the discriminator. The classifier shipped in #2614 resolves the name itself: NXDOMAIN after 19 attempts is the never-written branch, so the budget is NOT the bug and that run is not needed. **One missing credential explains this AND the add-on surface.** `AllCatalogAddOns` seeds all eighteen at catalog DEFAULTS. external-dns's schema carries an `apiToken` secret knob wired to `CF_API_TOKEN` through a secretKeyRef, defaulting to empty — which is correct for a secret. With no token the controller runs with the cloudflare provider and no credential, crashloops (Degraded on every cloud, #2717), and never writes the ArgoCD ingress record — which is exactly what this probe then fails on. Two symptoms, one cause. **The soak was never reached**, so this row makes no day-2 drift claim; the access assertion fails first. **Teardown was interrupted again — the second consecutive aws run — and the long pole is now identified:** the ACM certificate (`aws_acm_certificate.cf_alias`) was still destroying at 14m0s against the flat 15m in-test ceiling. Not the VPC, which is what aws/byo suggested: the certificate alone nearly exhausts the budget. Recorded in #2729, whose arithmetic shows that raising the ceiling ALONE makes the failure worse. **#2614's ACM sweep proved itself on precisely its case:** a killed destroy left a certificate mid-delete, and `aws acm list-certificates` returns 0 afterwards. Full enumeration after the run: 0 ACM certificates, 0 EKS clusters, 0 instances, 0 NAT gateways, 0 non-default VPCs, 0 load balancers, 0 Elastic IPs. | `demos/proofs/aws/20260826T012631Z` | #2717 |
| 2026-08-26 | 66b98126 | gcp | maxconfig | **FAIL** | Blocked on a MAINTAINER APPLY, not on a defect — the fix is already merged and has never been applied to the cloud. 33 minutes, died at `tofu apply`: `Error applying IAM policy for dns managedzone ...: googleapi: Error 403: The caller does not have permission, forbidden`, at `google_dns_managed_zone_iam_member.external_dns_dns[0]` (`workload-identity.tf:32`). **This exact failure was diagnosed and fixed yesterday.** #2564 measured the roles against the live API — only `roles/owner` carries `dns.managedZones.setIamPolicy`; `dns.admin`, `editor` and `dns.peer` do not — and proposed a one-permission custom role rather than reaching for owner. It was closed as superseded by **#2567, which MERGED at 2026-08-25T13:32Z** and includes `infra/gcp-e2e/e2e-nightly.tf`. **A custom role defined in tofu is not a role in GCP until someone applies it.** Verified against the live project: its custom roles are `alethiaE2eProjectReader`, `alethiaFirestoreProvisioner`, `alethiaProjectReader`, `alethiaPubSubProvisioner`, `alethiaServiceAccountProvisioner`, `alethiaStorageProvisioner` — no DNS-zone-IAM role among them — and `alethia-e2e-nightly@` still holds `roles/dns.admin`, the role #2564 proved insufficient. So the merged fix is inert until `infra/gcp-e2e` is applied, which agents are refused (CLAUDE.md non-negotiable: humans apply). **This blocks every gcp dimension that provisions a DNS zone** — maxconfig, and full — and nothing in this repo can clear it. gcp's floor, byo and day2 are unaffected and already proven, because a floor-shaped run creates no zone, which is exactly why this stayed invisible until a max-config run reached it. **Teardown verified independently of the sweeper:** 0 GKE clusters, 0 compute instances, 0 disks, 0 DNS managed zones, 0 Cloud SQL instances, 0 Memorystore instances. The failed apply rolled back cleanly. | `demos/proofs/gcp/20260826T025751Z` | #2567 |
| 2026-08-26 | 66b98126 | aws | byo | **RETRACTED** | Supersedes the 2026-08-26 aws/byo **FAIL** row at bundle `demos/proofs/aws/20260826T001739Z`. The verdict was right and the RATIONALE was wrong: it opens "Every assertion the dimension makes PASSED; the run failed on a teardown timeout", and that is false. The day-2 ACCESS assertion failed FIRST, with the same cause aws/day2 hit an hour later: `day-2 access assertion FAILED: ... argocd-url reachable=false — dns-not-resolving: the hostname does not resolve — external-dns has not written the record (waiting longer will NOT fix this) (after 19 attempt(s) over 3m0s)` at `t2_provision_test.go:636`. The teardown interruption is real and is recorded correctly, but it happened afterwards in `t.Cleanup` and is not the only failure. **How the error was made, because the shape will recur.** I grepped the context around `--- FAIL: TestT2RealCloudProvisioning` and found the teardown line immediately above it, and read adjacency as causation. A failing `tofu destroy` emits hundreds of `Still destroying...` lines, which push any earlier assertion failure far out of a context window — so on THIS harness the last thing before the FAIL marker is the least likely thing to be the cause. The correct read is to search for every `assertion FAILED` in the log, not the lines nearest the summary. **What actually stands from the superseded row:** A0.6 was proven; `addon-reloader` IS in the expected set, which is #2672 visible on a real run; the teardown enumeration and the identification of the surviving default-VPC internet gateway are unaffected; #2716's sweeper-name fix is visible. Those parts are re-asserted, not withdrawn. **What changes as a consequence:** the superseded row said "a re-run is cheap and likely green". It is not. aws/byo fails for the same reason aws/day2 does — external-dns has no credential and never writes the ArgoCD ingress record (#2591, #2717, #2734) — so re-driving it before that is fixed buys the same failure. I had this cell queued as the cheapest re-drive available and cancelled it on this evidence. | `demos/proofs/aws/20260826T001739Z` | #2591 |
| 2026-08-26 | 66b98126 | aws | byo | **FAIL** | Re-record of run 32909287152 with the cause its own log reports. TWO failures, in this order: **(1) day-2 access**, at `t2_provision_test.go:636` — `argocd-url reachable=false (https://argocd.32909287152-1.e2e.alethialabs.io) — dns-not-resolving: the hostname does not resolve — external-dns has not written the record`. Identical to aws/day2 an hour later, and traced to one cause: the e2e seeds all eighteen add-ons at catalog DEFAULTS, external-dns's `apiToken` knob is empty by design, so the controller runs the cloudflare provider with no credential, crashloops, and writes no record. **(2) the in-test teardown**, `tofu destroy failed: signal: interrupt` after `aws_internet_gateway.this[0]: Still destroying... 13m30s elapsed` against the flat 15m ceiling (#2729). A second, independent failure — not the cause of the first. **What PASSED and is worth keeping:** `A0.6: ArgoCD-with-repos proven — repo-apps + repo-byo Applications Healthy+Synced and managing real resources on real infra`, and the expected set `[addon-byo-e2e addon-reloader apps aws-load-balancer-controller cert-manager external-dns external-secrets-operator metrics-server]` contains `addon-reloader` — #2672's fix on a real run, where every earlier byo row asserted a set without it. **Teardown verified independently of the sweeper:** 0 EKS clusters, 0 running instances, 0 NAT gateways, 0 non-default VPCs, 0 Elastic IPs, 0 load balancers. One internet gateway survives and is IDENTIFIED, not assumed: `igw-0ab1e5b71d1708251`, attached to a VPC with `IsDefault` true, untagged, and a different id from the one this run was destroying (`igw-0f72ac7592e2d55d4`). | `demos/proofs/aws/20260826T001739Z` | #2591 |
| 2026-08-26 | ebe47c0c | aws | maxconfig | **PASS** | **The first aws/maxconfig PASS, and the first dedicated run of this cell** — every earlier verdict came via the `full` composite. All ELEVEN provisionable kinds proven in tofu state: `network cluster database cache queue topic nosql secrets bucket registry dns`, 0 carried as converged ArgoCD Applications. Receipt signed and sealed to plan `sha256:e22f1030…`; 172 resources applied; 963 log lines shipped to job_logs; 3 nodes Ready. **7/7 asserted ArgoCD Applications Healthy+Synced with 0 withheld** — including `external-dns`, which is what makes #2811 gcp-specific rather than universal: IRSA grants Route 53 access at a scope Cloud DNS has no equivalent for, so the zone-scoped-binding trap does not arise here. **Teardown independently verified**: `aws cleanup verified complete for run 32960217601-1 — no billable resources remain`, after the two consecutive interrupted teardowns recorded above. VERDICT.txt reads `destroyed=false (?)` because the in-test `tofu destroy` did not finish inside its own ceiling; the guaranteed scope-locked sweep did, and enumerated 0 ACM certificates and 0 Route 53 zones. **What this row does NOT claim**: `soak: skipped`, so no day-2 posture is asserted (the day2 access probe did surface an endpoint and reach the cluster, but the soak is that dimension's vehicle). A0.6 also passed here — repo-apps and repo-byo Healthy+Synced and managing real resources — but this run was dispatched as `maxconfig`, so the gitops cell keeps its own evidence rather than borrowing this one's. | `demos/proofs/aws/20260826T114712Z` | |
| 2026-08-27 | e70404fe | gcp | maxconfig | **FAIL** | **Not a maxconfig failure — the byo-iac leg took the cell down with it.** `E2E_BYO_IAC` is set as a repo variable, so that leg runs on EVERY dimension, and it died at `t2_provision_test.go:753: byo-iac: BYO DEPLOY terminal status = "FAILED", want SUCCESS`. The cause is one permission: `Error 403: Required 'compute.projects.setCommonInstanceMetadata' permission for 'projects/itgix-adp', forbidden` at `google_compute_project_metadata_item.drift_probe`. **Everything around it was already correct**, which is why this is worth recording rather than just fixing: the negative fixture was refused for the right reason and named the right line (`[provider-not-allowlisted] main.tf:23 provider "cloudflare/cloudflare"`), the drift module passed the static gate (`providers=[hashicorp/google], 0 local module(s), 0 finding(s)`), and the plan was right. Only the apply was impossible. **Granting the permission is the WRONG fix and was rejected**: common instance metadata includes `ssh-keys`, so that one verb grants SSH into every VM in a SHARED project — to the credential a CUSTOMER's own OpenTofu executes under. Same reasoning #2886 applied to `setIamPolicy`. The probe moved to a bucket label instead (alethialabs-io/enterprise-demo#5 + #2902); the e2e identity already holds `roles/storage.admin`, so the scoped probe needs no new grant at all. **What this run DOES prove:** the morning's `infra/gcp-e2e` apply works end to end — `google_service_account_iam_member.external_dns_wi` bound `itgix-adp.svc.id.goog[external-dns/external-dns-sa]` to the standing `alethia-e2e-external-dns@` identity, and `google_dns_managed_zone_iam_member.external_dns_dns` granted the per-run zone. #2811's adoption path is live. **Teardown verified independently of the sweeper:** `gcloud compute project-info describe` afterwards shows no `alethia-byo-iac-*` metadata key — the probe apply had 403'd, so there was nothing to reclaim. **A second defect, filed separately:** the guaranteed-teardown fallback could not clone (`git@github.com:… SSH agent requested but SSH_AUTH_SOCK not-specified`) even though the deploy cloned the same repo over https — #2905. Nothing leaked here, but a leg failing AFTER a successful apply would have a broken safety net. | artifact `e2e-proof-gcp-33051613329` | #2811 |
| 2026-08-27 | e70404fe | azure | maxconfig | **BLOCKED** | Environmental, not a product defect, and BLOCKED rather than FAIL because nothing in this repo can clear it. `azurerm_managed_redis` in **westeurope** was refused: `Code: "InsufficientCapacity"`, `Message: "Request failed due to insufficient capacity. Retry using a different Azure Managed Redis size or region."` — the apply reached the cache kind and stopped there. **#2884's external-dns fix is PROVEN by this run and is not what blocks the cell**: `azurerm_federated_identity_credential.external_dns[0]: Creation complete` with subject `system:serviceaccount:external-dns:external-dns-sa`, and the run died well past it on an unrelated resource. **Teardown reported a problem and the account is nevertheless clean** — the in-test destroy emitted `Error: deleting Resource Group "rg-alethia-nl-33051620405-1": the Resource Group still contains Resources`, so it was verified INDEPENDENTLY rather than trusted: `az group list` shows no group naming this run, `az resource list` shows zero resources naming it anywhere in the subscription, and the only survivors are the two standing groups (`alethia-e2e-meta`, `alethia-connector`) holding one permanent resource each. The scope-locked sweep did what the in-test destroy could not. | artifact `e2e-proof-azure-33051620405` | |
| 2026-08-27 | e70404fe | azure | maxconfig | **BLOCKED** | The westeurope retry, in **northeurope**, and it cost **nothing** — the pre-spend capacity preflight refused before any apply: `capacity preflight [REFUSE]: "Standard_E2s_v3" is NOT available in northeurope`. Recorded because a guard doing exactly its job is evidence worth keeping: the alternative is an apply that creates part of a cluster and then fails on capacity, which is what happened twice on 2026-08-25. The region was then chosen by ASKING the cloud rather than guessing — `az vm list-skus --size Standard_E2s_v3 --all` lists germanywestcentral, italynorth, polandcentral, swedencentral, switzerlandnorth and uksouth as unrestricted in Europe — and the next attempt went to swedencentral. | (no bundle — refused before spend) | |
| 2026-08-27 | e70404fe | hetzner | addons | **FAIL** | 7 of 19 Applications not Healthy+Synced, and for the first time the losers are named with their OutOfSync RESOURCES: `argo-rollouts` Healthy/OutOfSync (5 CRDs) · `kyverno` Healthy/OutOfSync (5 CronJobs) · `tempo` Healthy/OutOfSync (1 StatefulSet) · `loki` Progressing/OutOfSync (1 StatefulSet) · `harbor` Progressing/OutOfSync (3 StatefulSets) · `falco` Progressing/Synced · `minio` Degraded/Synced. **Two of them were not add-on defects at all — the pods never scheduled.** minio: `FailedScheduling 0/7 nodes are available: 1 node(s) had untolerated taint(s), 6 Insufficient memory`. The minio chart's default is `resources.requests.memory: 16Gi` and the catalog shipped it unexamined; a cpx32 node is 16 GB in TOTAL, so no node in any default pool on any cloud can hold it. loki is the same one level down — its chunks-cache memcached sizes from `chunksCache.allocatedMemory` (default 8192 MB) and asked for 9830Mi. Together they were 27 of the surface's 29.6 Gi. Fixed and guarded in #2909 (29.6 Gi → 5.7 Gi, largest pod 16384Mi → 1229Mi). **`harbor`'s row predates its fix** — #2893 merged after this run started. **The remaining four are still unattributed, and the reason is a harness bug, not patience:** the `argocd app diff` dump #2778 added to name the differing FIELD has never once executed. Every Application returned `Error from server (NotFound): deployments.apps "argocd-server" not found` — ArgoCD is installed as `helm upgrade --install argo-cd argo/argo-cd`, so the real Deployment is `argo-cd-argocd-server` (verified by rendering the pinned 8.6.4 chart). #2907 resolves it by label. Since #2778 is explicit that a GUESSED `ignoreDifferences` entry can mask real drift, no field is asserted here. **`velero` (Missing) and `vault` (Progressing) are declared needs-user-config exclusions, not defects**; `addon-external-dns` Degraded is the excluded marketplace add-on, and the PLATFORM external-dns rail correctly renders nothing on an addons run (no DNS zone ⇒ `.DNSEnabled` false). **`addon-byo-e2e` read Missing/OutOfSync in the add-on wait and Healthy/Synced later in the same log** — the harness's own `triggerArgoSync` runs in between, and it is the only thing in the codebase that syncs a BYO Application. Filed as #2910: production has no equivalent, so the e2e may be proving a path a customer does not have. **Teardown:** the guaranteed scope-locked sweep reports success; NOT independently enumerated, because reading the `HCLOUD_TOKEN` from the prod secret is policy-blocked in this session. Recorded as unverified rather than claimed. | artifact `e2e-proof-hetzner-33051626972` | #2717 |
| 2026-08-27 | e70404fe | azure | maxconfig | **FAIL** | The swedencentral attempt, and the one that got far enough to be informative. 57 resources applied and destroyed cleanly (`Destroy complete! Resources: 57 destroyed. Environment destroyed successfully!`) — so the westeurope `InsufficientCapacity` really was regional, and northeurope's preflight REFUSE really was about the VM SKU. **The byo-iac custody chain came within one step of complete:** `gate-blocks-bad=true` (the non-allowlisted `cloudflare` provider refused before any tofu ran) · `pinned-clone-logged=true` · **a signed receipt over the CUSTOMER's own plan**, sealed to `sha256:5ab182fab4be…` · `state-on-proxy=true(1 res)` — the HTTP state proxy is the sole sink and it is not empty · `no-cluster-output=true`, so the kubeconfig → CNI → ArgoCD tail correctly stayed off · `ctx=alethia-nl/33055404521-1`, which only the runner's frozen `TF_VAR_alethia_*` injection can produce. It died at `byo-iac: the module emitted no "drift_marker" output`. **The harness has always read that output and no module has ever declared one** — all five emit `alethia_context` and `drift_target` only — so the contract's baseline half was never implemented on any cloud, and this is simply the first run that ever reached the step. Fixed upstream in alethialabs-io/enterprise-demo#5. **A leak, found and reclaimed.** The BYO probe lives in a state SEPARATE from the cluster's, so the healthy 57-resource teardown says nothing about it — and the fallback destroy could not clone (#2905, the same scp-style `git@github.com:` URL the deploy had just fetched over https). `az group list` afterwards showed `alethia-byo-e2e33055` in swedencentral, empty, tagged `drift_marker: baseline` / `managed_by: alethia-byo-iac-e2e`. Deleted by hand and the deletion confirmed. Not money — an empty Azure resource group is free — but it is exactly the blast radius #2905 predicted, now demonstrated on a second cloud. | artifact `e2e-proof-azure-33055404521` | #2905 |
| 2026-08-27 | 7108a7e | hetzner | floor | **PASS** | hetzner: ✅ apply(18 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/hetzner/20260827T192915Z` | — |
| 2026-08-27 | 21d1051 | azure | byo-iac | **PASS** | azure: ✅ apply(29 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/azure/20260827T204358Z` | — |
| 2026-08-27 | 98bbef1 | hetzner | byo-iac | **PASS** | hetzner: ✅ apply(18 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/hetzner/20260827T210204Z` | — |
| 2026-08-27 | 98bbef1 | azure | maxconfig | **PASS** | azure: ✅ apply(57 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/azure/20260827T211849Z` | — |
| 2026-08-27 | 165bc4c | azure | floor | **PASS** | azure: ✅ apply(29 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/azure/20260827T215237Z` | — |
| 2026-08-28 | d465522 | gcp | byo-iac | **PASS** | gcp: ✅ apply(24 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/gcp/20260828T110456Z` | — |
| 2026-08-28 | 0bea00b | gcp | floor | **PASS** | gcp: ✅ apply(24 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/gcp/20260828T120037Z` | — |
| 2026-08-28 | 5f3a95e | gcp | maxconfig | **PASS** | gcp: ✅ apply(45 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/gcp/20260828T124233Z` | — |
| 2026-08-28 | 2e07a17 | aws | floor | **PASS** | aws: ✅ apply(125 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/aws/20260828T125612Z` | — |
| 2026-08-28 | f50f804 | aws | gitops | **PASS** | aws: ✅ apply(125 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted)→destroyed(1) | `demos/proofs/aws/20260828T142417Z` | — |
| 2026-08-28 | f75af2a | aws | byo-iac | **PASS** | aws: ✅ apply(125 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted, UNMEASURED)→destroyed(1) | `demos/proofs/aws/20260828T155743Z` | — |
| 2026-08-28 | e3c6ebc | aws | day2 | **PASS** | aws: ✅ apply(125 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (8/8 asserted)→destroyed(1) | `demos/proofs/aws/20260828T190408Z` | — |
| 2026-08-29 | 19cdde3 | hetzner | addons | **PASS** | hetzner: ✅ apply(33 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted, UNMEASURED)→destroyed(1) | `demos/proofs/hetzner/20260829T085104Z` | — |
| 2026-08-29 | d1af73c | gcp | addons | **PASS** | gcp: ✅ apply(24 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (T2-asserted, UNMEASURED)→destroyed(1) | `demos/proofs/gcp/20260829T093816Z` | ⚠️ argocd counts unmeasured: pre-#3281 binary (A0.6's convergence loop wrote no summary); the assertion DID run and pass — run 33243600150 logs `all 20 asserted ArgoCD Applications are Healthy+Synced (1 withheld)` |
| 2026-08-29 | f953e10 | hetzner | maxconfig | **PASS** | hetzner: ✅ apply(36 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (16/16 asserted)→destroyed(1) | `demos/proofs/hetzner/20260829T105705Z` | — |
| 2026-08-30 | 1b40c96 | azure | addons | **PASS** | azure: ✅ apply(? added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (20/20 asserted)→destroyed(?) | `demos/proofs/azure/20260830T005214Z` | — |
| 2026-08-30 | 5f1dd7a | aws | addons | **PASS** | aws: ✅ apply(115 added)→node Ready (T2-asserted)→ArgoCD Healthy+Synced (25/25 asserted)→destroyed(1) | `demos/proofs/aws/20260830T100243Z` | — |
| 2026-09-02 | 09e0954 | gcp | floor | **FAIL** | **Not a provisioning failure — it died at the post-deploy convergence gate.** `apply` completed (25 added), the GKE nodes went `Ready`, the signed receipt verified sealed to plan `48847a6b…`, and the verification gate passed 3/3. `20260902T085409Z/VERDICT.txt` reads `gcp: ❌ FAILED at stage 'argocd-ready'`, and `provision-summary.json` records `"deploy_stage": "argocd-ready"` with `resources_added: 25`. A0.6 convergence (`test/e2e/t2_provision_test.go:687`) ended **2 of 4 Applications short at the 8m0s deadline**: `addon-byo-e2e` at `health=Healthy sync=Unknown` with `ComparisonError: Failed to load target state: failed to generate manifest for source 1 of 1: rpc error: code = Unavailable desc = dns: A record lookup error: lookup argo-cd-argocd-repo-server on 10.2.0.10:53: dial udp 10.2.0.10:53: i/o timeout` — the application-controller could not resolve **its own repo-server** — and `addon-reloader` at `health=Progressing sync=Synced`, with the harness's own line recording that ArgoCD names no unhealthy resource under it. Neither workload was broken; only the COMPARISON failed, which is why one reads `sync=Unknown` rather than `OutOfSync`. `kubectl describe` records exactly two events across `addon-byo-e2e`'s entire 16-minute life (`-> Unknown`, `-> Healthy`): its sync status never once left `Unknown`, so **not one compare ever succeeded**. **It reproduced the next night, for free** — run `33731101952` (2026-09-03), on the UNCHANGED sha `09e0954`, same stage `argocd-ready`, same `8m0s` budget, same losing Application `addon-byo-e2e`, with `DeadlineExceeded ... context deadline exceeded` as the proximate error instead of the DNS timeout. Two proximate errors, one invariant: `addon-byo-e2e` never completes a single manifest render inside the window, while `apps` — the other repo-backed Application, same cluster, same run — reached `Synced` on both nights. That second red is what makes this a FAIL rather than a flake, and it supersedes the 2026-09-02T20:26 comment on #3855 that read it as one; the schedule had already spent the re-dispatch that comment asked for. **The green clouds exonerate the chart, the repo and the credential:** `aws`, `azure` and `hetzner` passed the SAME `addon-byo-e2e` assertion, on the SAME BYO chart, in the SAME run — azure against the identical 4-app set. **gcp is the only cloud whose floor node is under 4 GiB**: `e2-small`, 2 GiB shared-vCPU, ~800-900 Mi allocatable once the GKE system DaemonSets are placed, on a 20 GB `pd-standard` disk with Calico — against `t3.large` (8 GiB) on aws and `Standard_D2s_v3` (8 GiB) on azure. `argocd-repo-server` is the component that runs `helm template`, and it is the component that fails; a starved kube-dns dropping UDP replies and a starved repo-server blowing its own gRPC render deadline are exactly the two surfaces observed. `packages/core/argocd/versions.go` had already measured this node (`16.9 MB of redis took 6m29s of PULL — ~43 KB/s ... this disk is genuinely saturated`) and had already named the lever: *the next step after this one is NOT another minute of deadline — it is the node shape*. Neither dev-side change could reach these runs — the nightly runs `main` (`09e09542`, 2026-08-31); #3566's zonal switch is capacity-neutral by its own comment, and #3586's 12m30s converge budget was written for a different loser (`external-secrets-operator`) and has never executed against gcp: both nights printed `8m0s`. **An E2E-harness capacity defect on one cloud's cheapest node profile, not a product defect** — the control plane, runner, receipt signing, templates and BYO GitOps path all worked. Diagnosed offline from the run artifacts; NO re-run was bought for this row. | artifact `e2e-proof-gcp-33605830312`, bundle stamp `20260902T085409Z` | #3855 |
